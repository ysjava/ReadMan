<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.learn.ipctestserver">

    <permission
        android:name="com.learn.ipctestserver.CUSTOM_PERMISSION"
        android:description="@string/app_name"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nnsman">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name="com.learn.ipctestserver.test.IPCMessengerServer"
            android:enabled="true"
            android:exported="true"
            android:permission="com.learn.ipctestserver.CUSTOM_PERMISSION" />

    </application>

</manifest>